# WebAgent 部署环境变量配置示例

# 服务端口号
AGENT_PORT=3000

# 监听地址
AGENT_HOST=0.0.0.0

# 是否严格使用配置端口；生产环境默认 true，非生产环境默认 false。
# 可省略此变量，省略时由 NODE_ENV 自动推断。如需覆盖默认值，取消下行注释并按需修改。
# AGENT_STRICT_PORT=true

# 运行环境
NODE_ENV=production

# 允许跨域的源列表（多个用逗号分隔）
CORS_ORIGIN=http://localhost:3000
